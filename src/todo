 * Only submit Client.java & Serve.java (no other files/folders)
 * Change so Alice != alice
 * If user typed anything but yes/no for this prompt "Do you want to send a message? (yes/no)" and we gonna prompt it once again to
user till user types yes or no
* make it impossible to send an empty message. if user try to send empty message then user will be prompt ance again with the
 "Enter message: " prompt.
* Write only comments for code that is new (new been provide in the assignment or complex, ask ai)
* Check code for proper indentation / variables names, so they are meaningful
* my program has process where public ket use for encryption and then private key used for decryption
*
